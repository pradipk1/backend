{
  "name": "day03-(express)",
  "version": "1.0.0",
  "description": "* Previously, we had created a server using \"http\" which is a core module of Nodejs. But we can see that to create the whole backend using the core module will be very complex because we have to handle conditionally all the api end points and  methods like GET, POST, PUT etc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
